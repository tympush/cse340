<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%- messages() %>

<a href="../add-classification">Add New Classification</a>
<a href="../add-inventory">Add New Inventory</a>